<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaozhi.dao.UserDao">

    <resultMap type="com.xiaozhi.entity.User" id="UserMap">
        <result property="id" column="ID" jdbcType="VARCHAR"/>
        <result property="username" column="USERNAME" jdbcType="VARCHAR"/>
        <result property="password" column="PASSWORD" jdbcType="VARCHAR"/>
        <result property="photo" column="PHOTO" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          ID, USERNAME, PASSWORD, PHOTO
        from xiaozhi.user
        where ID = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          ID, USERNAME, PASSWORD, PHOTO
        from xiaozhi.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        ID, USERNAME, PASSWORD, PHOTO
        from xiaozhi.user
        <where>
            <if test="id != null and id != ''">
                and ID = #{id}
            </if>
            <if test="username != null and username != ''">
                and USERNAME = #{username}
            </if>
            <if test="password != null and password != ''">
                and PASSWORD = #{password}
            </if>
            <if test="photo != null and photo != ''">
                and PHOTO = #{photo}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into xiaozhi.user(ID,USERNAME, PASSWORD, PHOTO)
        values (#{id},#{username}, #{password}, #{photo})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into xiaozhi.user(USERNAME, PASSWORD, PHOTO)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.photo})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into xiaozhi.user(USERNAME, PASSWORD, PHOTO)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.photo})
        </foreach>
        on duplicate key update
        USERNAME = values(USERNAME) , PASSWORD = values(PASSWORD) , PHOTO = values(PHOTO)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update xiaozhi.user
        <set>
            <if test="username != null and username != ''">
                USERNAME = #{username},
            </if>
            <if test="password != null and password != ''">
                PASSWORD = #{password},
            </if>
            <if test="photo != null and photo != ''">
                PHOTO = #{photo},
            </if>
        </set>
        where ID = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from xiaozhi.user where ID = #{id}
    </delete>
    <!--查询单个-->
    <select id="queryByName" resultMap="UserMap">
        select
          ID, USERNAME, PASSWORD, PHOTO
        from xiaozhi.user
        where USERNAME = #{username}
    </select>

    <!--查询用户的关注话题-->
    <select id="queryAttention" resultType="com.xiaozhi.vo.AttentionVO">
        select
	        u.ID as USER_ID,t.ID as TOPIC_ID
	        from xiaozhi.user u inner join xiaozhi.attention a
            on u.ID=a.USER_ID
            inner join xiaozhi.topic t
            on t.ID=a.TOPIC_ID
            where u.ID = #{id}
    </select>

    <!--查询用户发表的话题-->
    <select id="queryQuiz" resultType="com.xiaozhi.vo.MyQuizVO">
        select
	        u.ID as USER_ID,t.ID as TOPIC_ID
	        from xiaozhi.user u inner join xiaozhi.myquiz m
            on u.ID=m.USER_ID
            inner join xiaozhi.topic t
            on t.ID=m.TOPIC_ID
            where u.ID = #{id}
    </select>

    <!--添加关注的问题-->
    <insert id="insertAttention" parameterType="com.xiaozhi.vo.AttentionVO">
        insert into xiaozhi.attention(USER_ID,TOPIC_ID)
        values (#{user_id},#{topic_id})
    </insert>

    <!--取消关注-->
    <insert id="delAttention">
       delete from xiaozhi.attention where USER_ID = #{user_id} and TOPIC_ID=#{topic_id}
    </insert>

    <!--删除发表-->
    <insert id="delQuiz">
       delete from xiaozhi.myquiz where USER_ID = #{user_id} and TOPIC_ID=#{topic_id}
    </insert>
</mapper>