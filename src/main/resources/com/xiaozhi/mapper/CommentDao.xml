<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaozhi.dao.CommentDao">

    <resultMap type="com.xiaozhi.entity.Comment" id="CommentMap">
        <result property="id" column="ID" jdbcType="VARCHAR"/>
        <result property="content" column="CONTENT" jdbcType="VARCHAR"/>
        <result property="userId" column="USER_ID" jdbcType="VARCHAR"/>
        <result property="topicId" column="TOPIC_ID" jdbcType="VARCHAR"/>
        <result property="prise" column="PRISE" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CommentMap">
        select
          ID, CONTENT, USER_ID, TOPIC_ID, PRISE
        from xiaozhi.comment
        where ID = #{id}
    </select>

    <select id="queryComment"  resultType="com.xiaozhi.vo.CommentVO">
       select c.ID,c.CONTENT,c.USER_ID,c.TOPIC_ID,c.PRISE,u.USERNAME,u.PHOTO
            from xiaozhi.topic t inner join xiaozhi.comment c
            on t.ID=c.TOPIC_ID
            inner join xiaozhi.user u
            on c.USER_ID=u.ID
            where t.ID=#{id}
            order by c.PRISE DESC
            limit 0 , #{limit}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="CommentMap">
        select c.ID,c.CONTENT,c.USER_ID,c.TOPIC_ID,c.PRISE,u.USERNAME,u.PHOTO
          ID, CONTENT, USER_ID, TOPIC_ID, PRISE
        from xiaozhi.topic t inner join xiaozhi.comment c
            on t.ID=c.TOPIC_ID
            where t.ID=#{id}
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="CommentMap">
        select
        ID, CONTENT, USER_ID, TOPIC_ID, PRISE
        from xiaozhi.comment
        <where>
            <if test="id != null and id != ''">
                and ID = #{id}
            </if>
            <if test="content != null and content != ''">
                and CONTENT = #{content}
            </if>
            <if test="userId != null and userId != ''">
                and USER_ID = #{userId}
            </if>
            <if test="topicId != null and topicId != ''">
                and TOPIC_ID = #{topicId}
            </if>
            <if test="prise != null">
                and PRISE = #{prise}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into xiaozhi.comment(ID, CONTENT, USER_ID, TOPIC_ID, PRISE)
        values (#{id}, #{content}, #{userId}, #{topicId}, #{prise})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into xiaozhi.comment(ID, CONTENT, USER_ID, TOPIC_ID, PRISE)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.content}, #{entity.userId}, #{entity.topicId}, #{entity.prise})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into xiaozhi.comment(ID, CONTENT, USER_ID, TOPIC_ID, PRISE)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.content}, #{entity.userId}, #{entity.topicId}, #{entity.prise})
        </foreach>
        on duplicate key update
        ID = values(ID) , CONTENT = values(CONTENT) , USER_ID = values(USER_ID) , TOPIC_ID = values(TOPIC_ID) , PRISE =
        values(PRISE)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update xiaozhi.comment
        <set>
            <if test="id != null and id != ''">
                ID = #{id},
            </if>
            <if test="content != null and content != ''">
                CONTENT = #{content},
            </if>
            <if test="userId != null and userId != ''">
                USER_ID = #{userId},
            </if>
            <if test="topicId != null and topicId != ''">
                TOPIC_ID = #{topicId},
            </if>
            <if test="prise != null">
                PRISE = #{prise},
            </if>
        </set>
        where ID= #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from xiaozhi.comment where ID= #{id}
    </delete>

    <!--查询总条数-->
    <delete id="queryNum">
        select count(id) from xiaozhi.comment
    </delete>

</mapper>